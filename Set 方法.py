# 返回差集   set.symmetric_difference   set.symmetric_difference.update 
# 返回交集   set.intersection(), set.intersection.update()
# 并集      set.union
# (本集有其他集没有的）all elements that are in this set but not the others.    set.difference(), set.difference.update()
